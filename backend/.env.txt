FLASK_ENV=development
SECRET_KEY=replace_with_a_random_secret
JWT_SECRET_KEY=replace_with_jwt_secret
DATABASE_URL=sqlite:///spmr.db

# Optional ML service (if your ML team has a service)
ML_SERVICE_URL=http://localhost:5001/predict

# Twilio (SMS) - sign up and fill these or leave blank to use SMTP
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1234567890

# SMTP fallback (Gmail example: use app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=your_app_password_or_pwd
ALERT_EMAIL_TO=doctor@example.com
